# Decision log

One line per decision that shapes v2. Append, do not rewrite history. Format:
date, decision, why, and the sprint or source that prompted it. Newest at the bottom.

Voice rules apply: no Oxford commas, no em or en dashes.

| Date | Decision | Why |
|---|---|---|
| 2026-07-01 | v2 is an evolution of v1, not a rewrite. v1 stays untouched and shippable. | Protect the working tool. A bad experiment never touches main. |
| 2026-07-01 | First serious version is a Claude Project single-call MVP with nine lenses applied internally. | Zero marginal cost, fastest path to a testable review. True multi-agent is staged. |
| 2026-07-01 | Readiness is driven by blocking issues, not by the mean of dimension scores. | A clean-looking deck with an unsupported core recommendation must not read as almost ready. |
| 2026-07-01 | Strict noise budget: short mode 3 fixes, deep mode 5, max 8 evidence-linked comments, max 5 lead questions. | Restraint is a first-class quality metric. Overload kills adoption and trust. |
| 2026-07-01 | Drop the withdrawn arXiv 2605.17554 rubric as an authority. Re-ground every dimension on a verified source. | The paper was withdrawn on 2026-05-30. Design integrity for an ex-consultant reviewer. |
| 2026-07-01 | Three review modes, not five: Short (team self-check), Deep (full pre-client review), Lead (project-lead view). Coaching folds into every mode's learning block. | Simpler to adopt and explain. Fewer things for a busy team to choose between. |
| 2026-07-01 | Added a blocking rule for wrong or unstable problem framing (ceiling R1). | Framing failure should cap readiness even when the deck is otherwise polished. |
| 2026-07-01 | Ungrounded headline number blocks readiness only when the recommendation depends on it. | Avoid over-blocking on a stray unsourced stat that does not drive the client decision. |
| 2026-07-01 | Evaluation cases are synthetic only for now. Expert pack sized for a full 60-minute review (contains the 30-minute version). | Zero confidentiality risk immediately. No expert lined up yet. |
| 2026-07-01 | v2 schema is a superset of v1: renamed scorecard.overall to diagnosticMean, added result value na for out-of-scope dimensions, added readiness, blockingIssues, strengths, comments, questionsForLead, learningNote, notAssessed and finding fields deliveryCritical, issueType, owner. | Carry the report and the readiness logic without breaking the v1 renderer path. |
| 2026-07-01 | Five synthetic cases built (01 to 05) with gold labels written before AI review. Three worked AI reviews (01, 03, 05) span broken, weak and strong decks. | Test discernment and restraint, not just catching faults. Satisfies the definition-of-done case bar. |
| 2026-07-02 | Built check-review-v2.js (standalone, not extending the renderer-coupled v1 script). All three worked reviews and both live runs pass it, including verbatim-quote checks. | Live outputs proved structurally stable, so the validator earns its keep immediately. Alternative rejected: waiting for the renderer, which would leave quote checks manual. |
| 2026-07-02 | Ran cases 02 and 04 live and blind (fresh-context runner, failure-mode headers stripped, gold unread until output existed). Both pass: readiness exact match, must-catch as top finding, safety clean. Scores in eval-runs/scoring-2026-07-02.md. | The Sprint 3 exit expectation held on the first true blind runs. |
| 2026-07-02 | Prompt: findings mooted by a higher-ranked blocking fix fold in or drop, and communication findings go first when the budget forces a choice. | Evidence: both live runs spent budget on title hygiene and on polishing a recommendation the top fix replaces (case 02 findings 3 and 5, case 04 comment 1, each an FP or low-value against gold). Alternative rejected: shrinking the deep budget to 3, which would cost real findings on evidence-rich decks. Test: re-run 02 and 04, expect 3 to 4 findings and no title comment on broken decks, and confirm case 05 still gets its two minor findings. |
| 2026-07-02 | Prompt: verbatim quotes may join line-wrapped input with a single space. Contract: the validator comparison is whitespace-normalized. | Evidence: both live runners independently flagged the verbatim rule against hard-wrapped input, one truncating quotes to cope. Alternative rejected: requiring embedded newlines, which makes quotes renderer-hostile. Test: check-review-v2.js passes quotes that join wraps. |
| 2026-07-02 | Prompt: with partial input, confidence is Medium when readiness rests only on quotable text, Low when the missing material could change the level. Missing evidence notes never allow High. | Evidence: identical blank fields produced Low on case 02 and Medium on case 04. Alternative rejected: always Low on any blank field, which punishes reviews of self-contained decks. Test: re-run both, expect Medium on both. |
| 2026-07-02 | Source register pass: S4 figures corrected to the working paper, S7 author fixed (Omachonu) with venue caveat, S8 dated January 2026 with domain caveat, S9 figure confirmed, S10 rescoped to AI-assisted instructor feedback (also in the one-pager). | An expert who knows Dell'Acqua cold would have caught the garbled citation. Fixes verified against primary records on 2026-07-02. |
| 2026-07-03 | Fresh-context independent scorer scored the full five-case set. eval-runs/scoring-2026-07-03.md replaces the orchestrator-scored 07-02 sheet as authoritative. | The 07-02 scores were orchestrator-scored after a session limit blocked the independent scorer. Independence is the acceptance requirement, so the fresh read supersedes it. |
| 2026-07-03 | Case 05 adjudicated: the live R2 with three major findings was over-escalation, not genuine criticals the gold missed. Gold stays R3. | The independent scorer and a contrarian both read the three findings on the merits: real observations, inflated severity. The deck explicitly frames year one as a deliberately small pilot (slides 2, 6), so sizing 5,000 pounds against total income is a minor not a blocker. Alternative rejected: moving the gold to R2, which would ratify severity inflation on the one restraint case. Adjudication note added to case-05.gold.md, pre-registered gold kept intact above it. |
| 2026-07-03 | Severity-calibration fix: prompt gains a SEVERITY block (critical, major, minor defined by whether closing the finding changes the client's decision), a deliveryCritical invariant for no-blocker decks and a no-blocker demotion floor. Rubric section B carries the same definition. | Root cause of the case 05 miss: major-vs-minor was undefined for a non-blocking finding, so a strong deck with budget got inflated to R2. General change, provably a no-op on the four decks that fired a blocking rule (the new clauses bind only when blockingIssues is empty). Alternative rejected: shrinking the deep noise budget, which would cost real findings on broken decks. Case 05 name was kept out of the rubric so a blind re-run stays clean. |
| 2026-07-03 | Case 05 re-run blind under the revised prompt: Nearly ready (R3), two minor findings, no invented criticals, passes check-review-v2.js. Independent fresh re-score confirms section E points 1 and 4. Full five-case set is green. | The fix held without over-correcting to zero findings: the run kept the genuine materiality observation and graded it minor. The runner miscomputed diagnosticMean (4.4 for 4.7), caught by the validator and corrected to match its own dimension scores, which is itself evidence of the diagnosticMean reproducibility gap. |
| 2026-07-03 | Expert pack samples repointed from the hand-worked reviews to the live outputs: case 01 and 03 live runs and the case 05 revised-prompt re-run, marked as live-generated. | The set cleared the harness, so the pack shows real blind tool output, not design-time hand-work. The case 05 over-escalation and fix is offered to the expert as evidence the eval loop works. |
| 2026-07-03 | Effectiveness review written (effectiveness-review-2026-07-03.md) with a fresh-context contrarian. Fitness verdict: expert or lead companion, not yet direct student-facing. | The user asked for a critical system review before a pilot. Largest open risk named: run-to-run stability is unmeasured (one run per case per prompt version). The contrarian's run-to-run variance evidence was corrected: the 02 and 04 differences were mostly the deliberate prompt revisions, not nondeterminism. |
| 2026-07-04 | Gold set extends to real past deliverables through a dedicated intake pipeline (12-real-deck-intake.md). The synthetic set stays frozen as the regression suite. | The eval was five synthetic decks with insider gold and no extraction noise, the top gap named by the effectiveness review. |
| 2026-07-04 | Gold labels for real cases are human-only: two labelers work blind then adjudicate. The AI never writes, drafts or suggests a gold label. | Machine-drafted gold would let the reviewer grade its own homework. |
| 2026-07-04 | Machine triage bands are sealed sampling machinery, kept from labelers until after adjudication. | The bands only buy a quality spread at selection time. Reading them before labeling would anchor the human gold. |
| 2026-07-05 | Local-mirror pivot: the Drive folder is downloaded and text-extracted locally, the Drive connector leaves the pipeline entirely. | 8 parallel agents on MCP reads burned a 5-hour session window in minutes. Local-text Sonnet agents cost roughly a quarter and append per file so a killed agent loses at most one row. |
| 2026-07-07 | Real case set fixed at 14: 4 strong, 5 middling, 5 weak per sealed triage, one case per client, numbering shuffled, weak band weighted toward decks with genuine draft artifacts. | The spread and family cap keep the set representative and the shuffle keeps the case sequence from leaking bands to labelers. |
| 2026-07-22 | Built the v2 renderer as one self-contained index.html with no build step, plus bundle-reviews.js to inline the eval-runs outputs. Three views off one JSON: student coaching, project lead, printable. | The v2 output was raw JSON, so nothing could be shown to anyone. Alternative rejected: fetching the JSON at runtime, which fails from file:// and would force a server on every demo. The renderer ports the check-review-v2.js checks and refuses to draw an invalid review, verified byte-identical to the CLI on all six bundled reviews. |
| 2026-07-22 | The renderer follows check-review-v2.js rather than the system prompt where the two disagree on deliveryCritical. | Prompt section SEPARATE DELIVERY FROM LEARNING allows one delivery-critical finding on a no-blocker R2 deck. Validator rule 8a allows none. The validator is the enforced contract, so the renderer matches it. The contradiction is real and pre-existing: resolve it in the pre-freeze prompt pass, do not let the renderer arbitrate it. |
| 2026-07-22 | Independent fresh-context re-score of the five post-fix outputs committed as eval-runs/scoring-2026-07-22.md. Verdict GREEN, derived without reading either prior sheet. | The only committed scoring artifact scored the pre-fix case 05 and concluded NOT GREEN, so the green claim rested on a prose line in progress.md. Closing that gap before a board demo is cheaper than defending it during one. |
| 2026-07-22 | Run-to-run stability measured: 15 runs, 5 cases, 3 each, frozen prompt, byte-identical input packs, all Opus 4.8. Sheet at eval-runs/stability/README.md. No pass threshold set. | The single most important pre-pilot number and it was an assumption. Result: readiness stable on 4 of 5 cases, stable finding core about 54 percent, and case 05 still lands R2 in one run of three, so the strong-deck over-escalation is intermittent rather than fixed. Setting a threshold is an owner judgment and the sheet deliberately declines to invent one. |
| 2026-07-22 | The two contract-invalid stability runs are kept uncorrected, and case-05-run-a is bundled into the renderer as the refuse-to-draw demo. | They are the measurement. Correcting them would destroy the 13-of-15 first-pass compliance figure, and the refusal path had no demo until one existed. |
| 2026-07-22 | Prompt defect logged, not yet fixed: 04 section A never lists the issueType enum, so 2 of 15 runs invented the value "analysis". | Mechanical omission, the prompt inlines every other controlled vocabulary. Fixing it now would invalidate the fifteen stability runs, so it goes into the pre-freeze prompt pass with plan items 1.5i and 1.5j. |
| 2026-07-23 | Case 05 stability extended from three runs to five. Five-run readiness is R2, R3, R3, R2, R2. The strong-deck over-escalation is the majority outcome, not one in three. | Three runs cannot tell a one-in-three event from a one-in-five one, and case 05 is the case the whole set's green is most fragile on. The two extra runs changed the reading from "intermittent" to "more often than not", which is a materially different pre-pilot risk. Scoring sheet 2026-07-22 is not invalidated: it scored a real R3 run. But green on case 05 now visibly depends on which run is scored. |
| 2026-07-23 | Built the blind labeling workstation (eval-cases-real/labeling/labeling-workstation.html), build priority 4. Enforces the PROTOCOL by construction: exactly one must-catch or the restraint box, every required section filled, before download unlocks. Exports harness section C shape, autosaves locally. | Labeling 14 real cases at 30 to 40 minutes each is the longest calendar item on the critical path. The tool removes the copy-a-template, name-the-file, remember-the-must-catch-rule friction without ever writing a judgment, which is the exact line the intake policy draws. Blindness is structural: it loads only the case file opened, with no path to any AI output or triage band. |
| 2026-07-23 | Committed reviewer-heuristics-intake.html, the orphaned expert-heuristics intake form, as expert-track tooling. | It was already built, self-contained and serverless, and it serves plan Q2 (expert calibration) by capturing a reviewer's blind heuristics before they see the rubric. Untracked build output is a liability, so it is tracked with the rest. |
| 2026-07-25 | Contract prose (03 section B) aligned with the redesigned rubric. It still read "if blockingIssues is empty, readiness is R2 when any finding is major, R3 when only minor", the pre-redesign major-count coupling. Rewritten to: empty blockingIssues defaults to R3, demote to R2 only for a single decision-changing gap named in mainReason, a no-blocker deck can carry one major and still be R3. | Doc-only fix, no behaviour change. The enforced validators already match the redesign: check-review-v2.js rule 8d enforces only all-minor-to-R3 and rule 8c the escalation floor, neither enforces any-major-to-R2, and the renderer validator matches. So a legitimate no-blocker R3 with one major already validates. Only the contract prose was stale and it now agrees with 01 section B and check-review-v2.js. Part of pre-freeze consistency. |
| 2026-07-25 | Gold labeling moves from two-labeler blind-then-adjudicate to a single human pass, at the team's capacity constraint. The team labels each case once: a calibration round on four cases done together first (the rubric-health check that replaces the two-reader stop rule), then the remaining ten labeled once each, blind. An AI mechanical consistency check runs on every finished gold and flags internal contradictions for the humans to resolve, never a label. Optional expert spot-check on two or three cases for external validity. PROTOCOL.md, calibration-batch.md, agreement-log.md and 12-real-deck-intake.md updated in place. | The team has capacity for one pass, not two. Single pass loses the inter-rater agreement number and a second reader's independent catch. The calibration round recovers the rubric-ambiguity signal, the AI consistency check recovers internal-contradiction catching, and the optional expert spot-check gives a light external-validity read. The residual loss (no inter-rater reliability figure, insider gold intensified) is real and is read into the baseline, not hidden. The no-AI-label rule is unchanged: the consistency check is mechanical and never proposes or sets a label. |
| 2026-07-25 | Golden-set labeling pipeline verified end to end, mechanics only, no labels produced. The labeling workstation's export format (its build function) was confirmed by inspection to match the adjudication workstation's parser exactly: header, Level, one-sentence reason, blocking rules, the numbered issues with severity meta and MUST-CATCH, and all five section titles. The adjudication workstation was runtime-tested with Playwright using two fabricated fixtures (fake case real-00, labelers T1 and T2): it parses both R-levels, computes the readiness delta (R2 versus R3 is one level apart), renders both sheets, gates the gold download until the human fills resolved readiness, reason, issues and resolution, and never pre-selects a label. Both integrity guards fire: two different cases refused, same labeler refused. | The human labeling is the long pole and about to start, so the tooling had to be confirmed not to block it. The AI role is to check the mechanics, never to produce a label: the test used fabricated fixtures on a non-existent case id and wrote nothing into eval-cases-real. |
| 2026-07-25 | Interim call on three runs, since reversed: Path A looked redundant because the first three current-package runs were all R3. Kept visible so the reversal is on record. Superseded by the five-run entry immediately below. | Three runs read as three of three R3, the same three-run trap the earlier case 05 measurement fell into. The fourth run reproduced the over-escalation. |
| 2026-07-25 | Over-escalation is reduced, not eliminated, and the Path A decision is re-opened for the owner. Five-run re-measurement of the current package on synthetic case 05 (sheet at eval-runs/stability/current-2026-07-25/README.md): readiness R3, R3, R2, R3, R3, correct in four and over-escalated in one, down from the superseded package's three in five. The one R2 (run h) promotes the 5-versus-8 percent launch-gate finding to major, the same finding adjudicated on 2026-07-03 as a real observation graded too high on a deliberate-pilot deck. Run h is also contract-invalid (deliveryCritical with no blocking issue). Path A was NOT applied in this session: applying it or accepting one in five is the owner's call, teed up with the real number. Not a freeze: 1.5i L-dimension and 1.5j blockingIssues still precede any freeze. | The 2026-07-23 rubric redesign cut over-escalation from three in five to one in five, a real improvement, but did not remove it: the residual is a severity wobble on one borderline finding whose R2 is arguable because run h does name a decision-change. The rubric's own philosophy resolves this boundary toward R3 with the lead as backstop and names the real-case baseline as the true test, which argues for accepting one in five. Path A argues for pushing it toward zero now. Both defensible, so it is an owner call. Path B (the readiness gate) already ensures no student sees the false R2 regardless. |
| 2026-07-25 | Prompt and rubric drift discovered and recorded. Commit 54171e5 (2026-07-24, a bulk sync) applied to 04 section A the issueType enum and a readiness rewrite (no-blocker readiness is Nearly ready by default and is NOT decided by counting major findings), and the rubric's 2026-07-23 redesign decoupled no-blocker readiness from the major count through a single decision-change test with a deterministic R3 default. Neither was decision-logged at the time, and the log still read issueType as not yet fixed (2026-07-22 entry). All of it postdates the seventeen stability runs, which section D of the stability sheet proves by their invented issueType value. So the case 05 three-of-five over-escalation is stale: it measures a superseded package. A blind re-measurement of the current package on synthetic case 05 (five runs, byte-identical pack, current prompt plus rubric, Opus 4.8) is underway. This entry also resolves the rubric's dangling See-decision-log reference for the 2026-07-23 redesign. | Significant calibration changes reached the working tree without a decision-log line or a re-measurement, the exact silent-behaviour-change failure mode the freeze discipline exists to prevent. Recording them and re-measuring the current package before any freeze is the correction. No prompt or rubric edit was made in this entry: it documents state and triggers measurement. |
| 2026-07-24 | S4 closed against the journal version, clearing the source-register backlog. The published Organization Science version (DOI 10.1287/orsc.2025.21838, 11 Mar 2026) confirms 12.2 percent more tasks, 25.1 percent faster and 19 percentage points less likely outside the frontier verbatim. It does not carry the more-than-40-percent quality or the 43-percent below-average figures as percentages: those are 2023 working paper abstract numbers, restated qualitatively in the journal. S4 claim and status updated to attribute them to the working paper only. | The register cited the journal DOI while quoting two working-paper-only percentages, which breaks its own never-a-blend rule and is exactly what an ex-consultant who knows this paper would catch. This was the last item gating the expert recruitment in plan item 2a. Blast radius checked: the two figures appear as a source claim only in the S4 entry, nowhere else in v2. |
| 2026-07-24 | Renderer readiness gate made real. Default (no URL param) is unchanged: all three tabs open, which the board demo relies on and the risk is accepted. A student build is reached with ?role=student, under which the lead and printable views are never rendered into the DOM, their tabs are removed, and any forced setView to them bounces to coaching. | The renderer separated readiness into the lead view but did not gate it: load() enabled every tab and the lead tab was one click from any student. The standing fitness verdict is lead companion, and the effectiveness review's top adoption risk is a student reading a false not-ready. This makes the lead-only readiness claim true when it needs to be, without a silent change to the demo path. Verified with Playwright: default shows three tabs and populated readiness, ?role=student shows only coaching with #readiness empty in the DOM and the forced-navigation bounce holding. This is Path B of the over-flagging decision, prepared and verified, not a substitute for a source-side severity fix (Path A). |
| 2026-07-25 | Built check-gold.js, the AI mechanical consistency checker for single-pass gold worksheets (the compensating control PROTOCOL.md step 4 called for). It parses the labeling-workstation export shape and reports only internal contradictions for a human to resolve: exactly one must-catch or a declared restraint case, a valid readiness level, readiness at or below any cited blocking-rule ceiling, a blocking rule present if readiness is R0 or R1, no critical issue under an R3, valid severities, dimensions and issue types, every section filled. It mirrors check-review-v2.js rules 8a-8d (a critical issue implies a blocking condition, a must-catch is never minor, the bottom two readiness levels need a blocking rule, no-blocker plus all-minor is R3). It never proposes, sets or changes a label. Tested against two fabricated real-00 fixtures (fixtures/real-00.gold.PASS.md reads CONSISTENT, fixtures/real-00.gold.FAIL.md reports contradictions) and a 17-assertion per-rule harness (fixtures/check-gold.test.sh, all pass) that covers both the blocking-present and blocking-absent branches. PROTOCOL.md step 4 and STATUS.md updated to the real command. No real case touched. | Single-pass labeling loses the second reader's independent catch, and this is the mechanical control that recovers internal-contradiction catching without the AI ever writing a judgment. It had to exist before any real gold is trusted or any baseline run happens. Local Node, no API cost. The no-AI-label rule holds by construction: the checker only compares fields the human already wrote against each other and never emits a label. |
| 2026-07-25 | Owner decision on the case 05 over-escalation: accept 1 in 5, do not apply Path A. The current package over-escalates the strong synthetic deck in 1 of 5 runs (down from 3 of 5 on the superseded package), the residual is a genuine borderline severity call the rubric's err-toward-R3 philosophy tolerates, and Path B (the readiness gate, shipped) already ensures no student sees the false R2. The drafted severity-consistency clause (Path A) is NOT applied. The real-case baseline becomes the true test of calibration. | Both accept and apply were defensible, so it was the owner's call, teed up with the real number. Accept was the standing recommendation: the improvement is already large, the residual is a real judgment on one finding, the student risk is contained by Path B, and adding another prompt change would mean another re-validation for a marginal gain. The pre-freeze prompt pass therefore contains only 1.5i and 1.5j, not Path A. |
| 2026-07-25 | Plan items 1.5i and 1.5j implemented as the single pre-freeze prompt pass (Path A excluded per the owner). 1.5i, the scorecard denominator fix: each dimension now carries a scope field (full, light, na) set from the artifact-type matrix in 01 section C; checksTotal is pinned to the four enumerated sub-checks (1 to 4), shrinking only as specific sub-checks resolve to na for the deck (a sub-check with no basis is na, never a pass); diagnosticMean averages full-scope dimensions only, so a light-touch dimension's non-comparable denominator can no longer corrupt the one number the report presents as objective (the exact bug the 2026-07-03 effectiveness review flagged, items a and b). 1.5j, the blocking fix: when more than one rule fires, exactly one blocking issue is primary, the binding one with the lowest, most severe ceiling that sets readiness, the rest secondary, and mainReason names the primary. Enforced in check-review-v2.js and the renderer index.html, both presence-gated so reviews written before this pass still validate under the old in-scope-mean rule (the board demo's embedded legacy review is unaffected). 01 rubric, 03 contract, 04 prompt updated in place; the short-mode Dimensions cell was corrected so mode no longer reassigns scope (scope is a fixed artifact-type property). The three canonical eval-cases fixtures were mechanically upgraded to the new shape and validate. Verified: check-review-v2.js 9 of 9 new-format assertions plus 3 of 3 legacy backward-compat, renderer 10 of 10 headless Playwright checks (legacy demo, new-format binding/secondary and light-touch UI, scope-mismatch rejection). NOT a freeze: the blind synthetic re-measurement (1.5k) and the version tag (1.5l) still precede any freeze. | This is the pre-freeze consistency pass the freeze discipline requires: the last two known mechanical calibration gaps closed in one edit with one re-measurement to follow, rather than dribbled out unmeasured (the drift failure recorded earlier this session). Presence-gated enforcement kept the migration safe against the account session limit: no intermediate state breaks the demo or the historical records, so a killed session cannot leave the tool half-migrated. |
| 2026-07-26 | Prompt frozen: freeze tag frozen-2026-07-26 (plan item 1.5l). Section A of 04, the rubric 01 and the contract 03 are frozen for the first real-case baseline, no edits until plan item 1.5o. The freeze rests on the nine-run blind regression at eval-runs/regression-1.5ij-2026-07-25/ under the 1.5i + 1.5j package, Opus 4.8, byte-identical stability input packs, fresh-context blind runners each reading one self-contained pack: readiness match 9 of 9 (case 01 R1, 02 R1, 03 R1, 04 R0, case 05 R3 in all five runs), every run valid under the new contract with verbatim quotes, and the case 05 strong-deck over-escalation did not reproduce (5 of 5 R3, diagnosticMean stable 4.5 to 4.8, against the pre-1.5i package's 4 of 5). The live runs exercised both new mechanics: case 01 marked exactly the Feasibility dimension light per the draft-deck matrix (1.5i), case 04 fired three rules and marked the Not-ready rule 1 primary with the rest secondary (1.5j). A freeze banner was added to 04 section A. | The freeze discipline requires a tagged version resting on a fresh measurement, not a silent prompt state. This is that measurement, and it clears the last pre-baseline gate: the two mechanical calibration fixes are in, verified in fixtures and in live blind runs, and the package holds calibration on the synthetic five. Five case 05 runs cannot prove over-escalation is eliminated (zero in five is consistent with a true rate up to about one in three), which is why the owner accepted one in five and the real-case baseline stays the true test. What the freeze asserts is narrower and honest: the package is valid, calibrated on the synthetic set and stable enough to measure against the human gold. HARD RULE unchanged: no reviewer run on any real case before its gold exists. |
